package Controller;
import javafx.fxml.FXML;
import javafx.scene.control.Tab;
import javafx.scene.control.TabPane;

public class Controller {
    @FXML
    private TabPane tabPane;
 /*   @FXML private Tab pi;                              This will be adjusted later used to track tabs
    @FXML private PatientInfo PatientInfoController;
    @FXML private Tab images;
    @FXML private PatientInfo barTabPageController;
    @FXML private Tab wl;
    @FXML private WorkList fooTab1PageController;
    @FXML private Tab appt;
    @FXML private WorkList fooTab2PageController;
    @FXML private Tab te;
    @FXML private TechEntry fooTab3PageController;
    @FXML private Tab fi;
    @FXML private Files fooTab4ageController;
    @FXML private Tab rr;
    @FXML private Files fooTab5ageController;*/
    /*
    public void init() {
        tabPane.getSelectionModel().selectedItemProperty().addListener((ObservableValue<? extends Tab> observable,
                                                                        Tab oldValue, Tab newValue) -> {
            if (newValue == barTab) {
                //use to track tabs
                //System.out.println("Bar Tab page");
                barTabPageController.handleButton();
            } else if (newValue == fooTab) {
                //use to track tabs
                //System.out.println("Foo Tab page");
                fooTabPageController.handleButton();
            }
            else if (newValue == fooTab1) {
                //use to track tabs
                //System.out.println("Foo Tab page");
                fooTab1PageController.handleButton();
            }
        });
    }*/
}
